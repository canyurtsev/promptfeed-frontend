const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function run() { const user = await prisma.user.findFirst(); const prompt = await prisma.prompt.create({ data: { userId: user.id, title: 'Agentic Product Description Generator', description: 'Generates high-conversion product descriptions using structured reasoning and target audience adaptation.', content: 'Write a high-conversion product description for {{product_name}} targeting {{audience}}.\nInclude:\n- emotional hook\n- 3 key benefits\n- CTA\n- tone: persuasive but natural', tags: 'marketing, ecommerce, copywriting, agentic', isPremium: false } }); console.log(prompt.id); } run().finally(() => prisma.$disconnect());
